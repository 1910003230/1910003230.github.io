title: Step by step tutorial
baseurl: ""
url: "https://1910003230.github.io"

collections:
  authors:
    output: true

defaults:
  - scope:
      path: "/"
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: "/"
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: "/"
    values:
      layout: "default"